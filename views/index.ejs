<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Combat manager</title>

        <link rel="stylesheet" href="/css/bootstrap.min.css">
        <link async rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
        <style>
            body        { padding-top:80px; }
        </style>
    </head>
    <body>
        <div class="container">

            <div class="jumbotron text-center">
                <h1><span class="fa fa-superpowers"></span> DM Manager 5e</h1>

                <hr>
                
                <!-- show any messages that come back with authentication -->
                <% if (message.length > 0) { %>
                    <div class="alert alert-danger"><%= message %></div>
                <% } %>

                <!-- LOGIN FORM -->
                <form action="/login" method="post" style="width: 40%; margin: 0 auto">
                    <div class="form-group">
                        <label>Email</label>
                        <input type="text" class="form-control" name="email">
                    </div>
                    <div class="form-group">
                        <label>Password</label>
                        <input type="password" class="form-control" name="password">
                    </div>

                    <button type="submit" class="btn btn-warning btn-lg">Login</button>
                </form>

                <hr>

                <p>Login or Register with:</p>

                <!-- <a href="/login" class="btn btn-secondary"><span class="fa fa-user"></span> Local Login</a> -->
                <a href="/signup" class="btn btn-secondary"><span class="fa fa-user"></span> Local Signup</a>
            </div>

        </div>
    </body>
</html>